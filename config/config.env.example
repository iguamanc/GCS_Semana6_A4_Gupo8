# ParqueAndo - Variables de entorno (EJEMPLO)
# Copia este archivo a config.env (local) y reemplaza con tus valores.
# NO subas credenciales reales al repositorio.

APP_NAME=ParqueAndo
APP_ENV=dev
APP_PORT=3000

# Base de datos (ejemplo)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=parqueando
DB_USER=parqueando_user
DB_PASSWORD=change_me

# Seguridad (ejemplo)
JWT_SECRET=change_me